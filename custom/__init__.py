"""MAA custom action/recognition 实现包。

本包只放 MaaFramework pipeline 用的 custom action（custom.action）与
custom recognition（custom.reco），以及 agent 子进程入口（custom.agent）。
应用主体（UI / 计时 / 打轴 / 测量 / 资源）在 aao 包。
interface.json 的 child_exec 启动 custom.agent。
"""
